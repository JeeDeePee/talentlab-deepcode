query ModuleGoalsQuery($slug: String!) {
  modules(slug: $slug) {
    edges {
      node {
        slug
        goalSet {
          edges {
            node {
              id
              text
              level
            }
          }
        }
      }
    }
  }
}
