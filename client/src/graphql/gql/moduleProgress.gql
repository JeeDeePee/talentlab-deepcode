query ModuleProgressQuery($id: ID!) {
  moduleProgress(id: $id) {
    id
    username
    firstName
    lastName
    usermoduleSet {
      edges {
        node {
          id
          module {
            id
            category
            title
            teaser
            skill
            slug
            units {
              edges {
                node {
                  id
                  title
                  type
                }
              }
            }
          }
        }
      }
    }
  }
}
